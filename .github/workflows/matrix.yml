name: Matrix

on:
  workflow_dispatch:

jobs:
  greeting_job:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 1
      matrix:
        config:
        - stackName: edl-delta-loader-infra
          region: us-east-1
          currentEnvName: devl
          s3Bucket: jd-us01-commoninformationservices-devl-system
          fileName: infrastructure/cloudformation_templates/infrastructure-stack.j2
        - stackName: edl-files-loader-app
          region: us-east-1
          currentEnvName: devl
          s3Bucket: jd-us01-commoninformationservices-devl-system
          fileName: infrastructure/cloudformation_templates/application-stack.j2
        lambdas: [edl-file-ingest-completion-lambda , edl-delta-request-handler-lambda ]

    steps:
    - name: Deploy and Test - ${{  matrix.currentEnvName }} - ${{ matrix.stackName }}"
      run: |
         echo ${{ matrix.region}}
         echo ${{ matrix.currentEnvName}}
         echo ${{ matrix.s3Bucket}}
         echo ${{ matrix.fileName}}
         echo ${{ matrix.stackName}}
    - name: print1
      run: |
         echo ${{ matrix.lambda}}    

